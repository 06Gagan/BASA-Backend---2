<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5" style="max-width: 400px;">
      <h1 class="text-center"><%= title %></h1>
      
      <!-- Alert Section -->
      <% if (message) { %>
        <div class="alert <%= message.type === 'success' ? 'alert-success' : 'alert-danger' %> text-center">
          <%= message.text %>
        </div>
      <% } %>
      
      <!-- Reset Password Form -->
      <% if (token) { %>
        <form action="/reset-password" method="POST">
          <!-- Correctly pass only the raw token -->
          <input type="hidden" name="token" value="<%= token %>" />
          <div class="form-group">
            <label>New Password:</label>
            <input
              type="password"
              name="newPassword"
              class="form-control"
              required
              placeholder="Enter new password"
            />
          </div>
          <div class="form-group">
            <label>Confirm Password:</label>
            <input
              type="password"
              name="confirmPassword"
              class="form-control"
              required
              placeholder="Confirm your password"
            />
          </div>
          <button type="submit" class="btn btn-primary btn-block">Reset Password</button>
        </form>
      <% } else { %>
        <p class="text-danger text-center">Invalid or missing token. Please request a new password reset.</p>
      <% } %>
    </div>
  </body>
</html>
